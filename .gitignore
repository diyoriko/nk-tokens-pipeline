node_modules/
build/
storybook-static/
guide-dist/
*.log
.DS_Store
foundations/_audit/*.html
foundations/_archive/
*.woff2
.wrangler/
.claude/
*.tgz


# local review reports (kept out of the repo)
reports/

# npm auth — never commit a registry credential
.npmrc

# working backlog — local only (public repo until the Bitbucket move)
BACKLOG.md
